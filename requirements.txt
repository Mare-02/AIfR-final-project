# PDDLego — Abhaengigkeiten (Stand: Schritt 0, verifiziert unter Python 3.12.12, macOS arm64)
#
# TAMPanda wird NICHT hier gepinnt, sondern editierbar aus dem lokalen Klon
# installiert — siehe SETUP.md. Pfad ist bei jedem Teammitglied anders.

# --- Planung ---
unified_planning==1.3.0
up_fast_downward==0.5.2
up-pyperplan==1.1.0
pyperplan==2.1

# --- Simulation (kommt als TAMPanda-Abhaengigkeit, hier gepinnt fuer Reproduzierbarkeit) ---
mujoco==3.11.0
mink==1.2.0
numpy==2.5.1
scipy==1.18.0

# --- Benchmark-Parsing ---
PyYAML==6.0.3

# --- Entwicklung ---
pytest==9.1.1
